---
title: "{{< iconify ph clipboard-text >}} Prestatie"
format: html
---


```{=html}
<script>document.body.classList.add('has-pagination-top');</script>
<nav class='pagination-nav pagination-top' role='navigation' aria-label='Page navigation'>
  <div class='pagination-container'>
    <button class='pagination-btn pagination-prev pagination-disabled' disabled aria-label='Previous page'>
      <svg class='pagination-icon' width='18' height='18' viewBox='0 0 20 20' fill='none' xmlns='http://www.w3.org/2000/svg'>
        <path d='M12 16L6 10L12 4' stroke='currentColor' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'/>
      </svg>
    </button>
    <div class='pagination-info'>
      <span class='pagination-prefix'>Page </span>
      <input type='number' class='pagination-input' id='page-input-top' min='1' max='3' value='1' aria-label='Current page'>
      <span class='pagination-separator'> / 3</span>
    </div>
    <a href='prestatie_p2.html' class='pagination-btn pagination-next' aria-label='Next page'>
      <svg class='pagination-icon' width='18' height='18' viewBox='0 0 20 20' fill='none' xmlns='http://www.w3.org/2000/svg'>
        <path d='M8 16L14 10L8 4' stroke='currentColor' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'/>
      </svg>
    </a>
  </div>
</nav>

<!-- Pagination Navigation Script -->
<script>
(function() {
  const pageUrls = ["prestatie.html","prestatie_p2.html","prestatie_p3.html"];
  const pageInput = document.getElementById('page-input-top');
  
  if (pageInput) {
    pageInput.addEventListener('change', function() {
      const pageNum = parseInt(this.value);
      if (pageNum >= 1 && pageNum <= pageUrls.length) {
        window.location.href = pageUrls[pageNum - 1];
      } else {
        this.value = this.getAttribute('value');
      }
    });
    
    pageInput.addEventListener('keypress', function(e) {
      if (e.key === 'Enter') {
        this.blur();
      }
    });
  }
})();
</script>
```



```{r}
#| include: false
library(dashboardr)
```

**DIT IS EEN MOCKUP VERSIE GELIEVE NIET TE CITEREN**

De Performance-sectie meet de praktische digitale vaardigheden van deelnemers aan de hand van uiteenlopende realistische taken. In plaats van zelfrapportage testen deze items wat mensen daadwerkelijk kunnen: bijvoorbeeld betrouwbare informatie opzoeken, AI-gegenereerde beelden herkennen, hun apparaten beveiligen of AI-tools effectief gebruiken. De items bestrijken tien domeinen van digitale competentie, van strategische en kritische informatievaardigheden tot AI- en generatieve-AI-vaardigheden, en geven zo een totaalbeeld van hoe goed mensen zich in de digitale omgeving van vandaag kunnen bewegen.


```{r, echo=FALSE, results='asis'}
dashboardr::enable_modals()
```

```{r setup}
#| echo: false
#| warning: false
#| message: false
#| error: false
#| results: 'hide'

# Load required libraries
library(dashboardr)
library(dplyr)
library(highcharter)

# Global chunk options
knitr::opts_chunk$set(
  echo = FALSE,
  warning = FALSE,
  message = FALSE,
  error = FALSE,
  fig.width = 12,
  fig.height = 8,
  dpi = 300
)

# Load data from dataset_4014obs.rds
data <- readRDS('dataset_4014obs.rds')

# Data summary
cat('Dataset loaded:', nrow(data), 'rows,', ncol(data), 'columns\n')

# Create filtered datasets
# Each filter is applied once and reused across visualizations

data_filtered_984a0efe <- data %>% dplyr::filter(wave == 1)
data_filtered_4af682fd <- data %>% dplyr::filter(wave == 2)

```


```{r, echo=FALSE, message=FALSE, warning=FALSE, results='asis'}
# Use dashboardr's loading overlay function
dashboardr::add_loading_overlay("Loading", 1, theme = "light")
```


```{r, echo=FALSE, results='asis'}
dashboardr::modal_content(
  modal_id = 'PDCCS1R',
  text = '<h2>Digitale Inhoudscreatie & Samenwerking: Prestatie Vragen</h2>
<img src="https://placehold.co/600x400/EEE/31343C" style="max-width:100%; height:auto;">
<p>Deze modal biedt aanvullende context over de prestatievragen voor digitale inhoudscreatie en samenwerking.</p>'
)
```

## {{< iconify ph magnifying-glass >}} Strategische Informatie


**Strategische Informatievaardigheden** beoordelen het vermogen om effectief te zoeken naar en informatie online te vinden. Dit omvat het kiezen van goede zoekwoorden, het gebruiken van zoekfuncties en het vinden van antwoorden op vragen op internet.

```{r, echo=FALSE, message=FALSE, warning=FALSE}
create_blockquote("Prestatietaken: proportie correct of geselecteerd. Bij items met meerdere keuzes tonen we het aandeel dat elke actie selecteert.", preset = "question")
```
[{{< iconify ph cards >}} Bekijk alle Strategische Informatie resultaten](strategic_information.html)


::: {.panel-tabset}

### {{< iconify ph number-circle-one-fill >}} Ronde 1


::: {.panel-tabset}

##### {{< iconify ph users-fill >}} Algemeen


```{r perf-sis-wave1-overall}
# Hoe zorg je ervoor dat je alleen Nederlandse bronnen te zien krijgt? Klik op de plaats in de afbeelding op de plek waar je dit kunt aangeven.
result <- create_stackedbars(
  data = data_filtered_984a0efe %>% tidyr::drop_na(PSIS2R),
  title = "Hoe zorg je ervoor dat je alleen Nederlandse bronnen te zien krijgt? Klik op de plaats in de afbeelding op de plek waar je dit kunt aangeven.",
  questions = "PSIS2R",
  question_labels = "Hoe zorg je ervoor dat je alleen Nederlandse bronnen te zien krijgt? Klik op de plaats in de afbeelding op de plek waar je dit kunt aangeven.",
  stacked_type = "percent",
  color_palette = c("#3D7271", "#E28D50", "#F5D76E", "#C7E6D5", "#0F6B5A", "#BABACD"),
  horizontal = TRUE,
  x_label = "",
  stack_breaks = c(0, 10, 20, 30),
  stack_bin_labels = c("Incorrect", "Correct"),
  stack_map_values = list("1" = "Correct", "0" = "Incorrect"),
  stack_order = c("Correct", "Incorrect"),
  stack_label = NULL,
  weight_var = "weging_GAMO"
)

result
```


##### {{< iconify mdi:human-male-male-child >}} Leeftijd


```{r perf-sis-wave1-age}
# Hoe zorg je ervoor dat je alleen Nederlandse bronnen te zien krijgt? Klik op de plaats in de afbeelding op de plek waar je dit kunt aangeven.
result <- create_stackedbar(
  data = data_filtered_984a0efe %>% tidyr::drop_na(AgeGroup, PSIS2R),
  title = "Hoe zorg je ervoor dat je alleen Nederlandse bronnen te zien krijgt? Klik op de plaats in de afbeelding op de plek waar je dit kunt aangeven.",
  stacked_type = "percent",
  horizontal = TRUE,
  stack_breaks = c(0, 10, 20, 30),
  stack_bin_labels = c("Incorrect", "Correct"),
  stack_map_values = list("1" = "Correct", "0" = "Incorrect"),
  stack_order = c("Correct", "Incorrect"),
  color_palette = c("#3D7271", "#E28D50", "#F5D76E", "#C7E6D5", "#0F6B5A", "#BABACD"),
  weight_var = "weging_GAMO",
  x_var = "AgeGroup",
  stack_var = "PSIS2R"
)

result
```


##### {{< iconify mdi gender-transgender >}} Geslacht


```{r perf-sis-wave1-gender}
# Hoe zorg je ervoor dat je alleen Nederlandse bronnen te zien krijgt? Klik op de plaats in de afbeelding op de plek waar je dit kunt aangeven.
result <- create_stackedbar(
  data = data_filtered_984a0efe %>% tidyr::drop_na(geslacht, PSIS2R),
  title = "Hoe zorg je ervoor dat je alleen Nederlandse bronnen te zien krijgt? Klik op de plaats in de afbeelding op de plek waar je dit kunt aangeven.",
  stacked_type = "percent",
  horizontal = TRUE,
  stack_breaks = c(0, 10, 20, 30),
  stack_bin_labels = c("Incorrect", "Correct"),
  stack_map_values = list("1" = "Correct", "0" = "Incorrect"),
  stack_order = c("Correct", "Incorrect"),
  color_palette = c("#3D7271", "#E28D50", "#F5D76E", "#C7E6D5", "#0F6B5A", "#BABACD"),
  weight_var = "weging_GAMO",
  x_var = "geslacht",
  stack_var = "PSIS2R"
)

result
```


##### {{< iconify ph graduation-cap-fill >}} Opleidingsniveau


```{r perf-sis-wave1-edu}
# Hoe zorg je ervoor dat je alleen Nederlandse bronnen te zien krijgt? Klik op de plaats in de afbeelding op de plek waar je dit kunt aangeven.
result <- create_stackedbar(
  data = data_filtered_984a0efe %>% tidyr::drop_na(Education, PSIS2R),
  title = "Hoe zorg je ervoor dat je alleen Nederlandse bronnen te zien krijgt? Klik op de plaats in de afbeelding op de plek waar je dit kunt aangeven.",
  stacked_type = "percent",
  horizontal = TRUE,
  stack_breaks = c(0, 10, 20, 30),
  stack_bin_labels = c("Incorrect", "Correct"),
  stack_map_values = list("1" = "Correct", "0" = "Incorrect"),
  stack_order = c("Correct", "Incorrect"),
  color_palette = c("#3D7271", "#E28D50", "#F5D76E", "#C7E6D5", "#0F6B5A", "#BABACD"),
  weight_var = "weging_GAMO",
  x_var = "Education",
  stack_var = "PSIS2R"
)

result
```


##### {{< iconify ph globe-hemisphere-east >}} Migratieachtergrond


```{r perf-sis-wave1-mig}
# Hoe zorg je ervoor dat je alleen Nederlandse bronnen te zien krijgt? Klik op de plaats in de afbeelding op de plek waar je dit kunt aangeven.
result <- create_stackedbar(
  data = data_filtered_984a0efe %>% tidyr::drop_na(MigrationBackground, PSIS2R),
  title = "Hoe zorg je ervoor dat je alleen Nederlandse bronnen te zien krijgt? Klik op de plaats in de afbeelding op de plek waar je dit kunt aangeven.",
  stacked_type = "percent",
  horizontal = TRUE,
  stack_breaks = c(0, 10, 20, 30),
  stack_bin_labels = c("Incorrect", "Correct"),
  stack_map_values = list("1" = "Correct", "0" = "Incorrect"),
  stack_order = c("Correct", "Incorrect"),
  color_palette = c("#3D7271", "#E28D50", "#F5D76E", "#C7E6D5", "#0F6B5A", "#BABACD"),
  weight_var = "weging_GAMO",
  x_var = "MigrationBackground",
  stack_var = "PSIS2R"
)

result
```


:::


### {{< iconify ph number-circle-two-fill >}} Ronde 2


::: {.panel-tabset}

##### {{< iconify ph users-fill >}} Algemeen


```{r perf-sis-wave2-overall}
# Hoe zorg je ervoor dat je alleen Nederlandse bronnen te zien krijgt? Klik op de plaats in de afbeelding op de plek waar je dit kunt aangeven.
result <- create_stackedbars(
  data = data_filtered_4af682fd %>% tidyr::drop_na(PSIS2R),
  title = "Hoe zorg je ervoor dat je alleen Nederlandse bronnen te zien krijgt? Klik op de plaats in de afbeelding op de plek waar je dit kunt aangeven.",
  questions = "PSIS2R",
  question_labels = "Hoe zorg je ervoor dat je alleen Nederlandse bronnen te zien krijgt? Klik op de plaats in de afbeelding op de plek waar je dit kunt aangeven.",
  stacked_type = "percent",
  color_palette = c("#3D7271", "#E28D50", "#F5D76E", "#C7E6D5", "#0F6B5A", "#BABACD"),
  horizontal = TRUE,
  x_label = "",
  stack_breaks = c(0, 10, 20, 30),
  stack_bin_labels = c("Incorrect", "Correct"),
  stack_map_values = list("1" = "Correct", "0" = "Incorrect"),
  stack_order = c("Correct", "Incorrect"),
  stack_label = NULL,
  weight_var = "weging_GAMO"
)

result
```


##### {{< iconify mdi:human-male-male-child >}} Leeftijd


```{r perf-sis-wave2-age}
# Hoe zorg je ervoor dat je alleen Nederlandse bronnen te zien krijgt? Klik op de plaats in de afbeelding op de plek waar je dit kunt aangeven.
result <- create_stackedbar(
  data = data_filtered_4af682fd %>% tidyr::drop_na(AgeGroup, PSIS2R),
  title = "Hoe zorg je ervoor dat je alleen Nederlandse bronnen te zien krijgt? Klik op de plaats in de afbeelding op de plek waar je dit kunt aangeven.",
  stacked_type = "percent",
  horizontal = TRUE,
  stack_breaks = c(0, 10, 20, 30),
  stack_bin_labels = c("Incorrect", "Correct"),
  stack_map_values = list("1" = "Correct", "0" = "Incorrect"),
  stack_order = c("Correct", "Incorrect"),
  color_palette = c("#3D7271", "#E28D50", "#F5D76E", "#C7E6D5", "#0F6B5A", "#BABACD"),
  weight_var = "weging_GAMO",
  x_var = "AgeGroup",
  stack_var = "PSIS2R"
)

result
```


##### {{< iconify mdi gender-transgender >}} Geslacht


```{r perf-sis-wave2-gender}
# Hoe zorg je ervoor dat je alleen Nederlandse bronnen te zien krijgt? Klik op de plaats in de afbeelding op de plek waar je dit kunt aangeven.
result <- create_stackedbar(
  data = data_filtered_4af682fd %>% tidyr::drop_na(geslacht, PSIS2R),
  title = "Hoe zorg je ervoor dat je alleen Nederlandse bronnen te zien krijgt? Klik op de plaats in de afbeelding op de plek waar je dit kunt aangeven.",
  stacked_type = "percent",
  horizontal = TRUE,
  stack_breaks = c(0, 10, 20, 30),
  stack_bin_labels = c("Incorrect", "Correct"),
  stack_map_values = list("1" = "Correct", "0" = "Incorrect"),
  stack_order = c("Correct", "Incorrect"),
  color_palette = c("#3D7271", "#E28D50", "#F5D76E", "#C7E6D5", "#0F6B5A", "#BABACD"),
  weight_var = "weging_GAMO",
  x_var = "geslacht",
  stack_var = "PSIS2R"
)

result
```


##### {{< iconify ph graduation-cap-fill >}} Opleidingsniveau


```{r perf-sis-wave2-edu}
# Hoe zorg je ervoor dat je alleen Nederlandse bronnen te zien krijgt? Klik op de plaats in de afbeelding op de plek waar je dit kunt aangeven.
result <- create_stackedbar(
  data = data_filtered_4af682fd %>% tidyr::drop_na(Education, PSIS2R),
  title = "Hoe zorg je ervoor dat je alleen Nederlandse bronnen te zien krijgt? Klik op de plaats in de afbeelding op de plek waar je dit kunt aangeven.",
  stacked_type = "percent",
  horizontal = TRUE,
  stack_breaks = c(0, 10, 20, 30),
  stack_bin_labels = c("Incorrect", "Correct"),
  stack_map_values = list("1" = "Correct", "0" = "Incorrect"),
  stack_order = c("Correct", "Incorrect"),
  color_palette = c("#3D7271", "#E28D50", "#F5D76E", "#C7E6D5", "#0F6B5A", "#BABACD"),
  weight_var = "weging_GAMO",
  x_var = "Education",
  stack_var = "PSIS2R"
)

result
```


##### {{< iconify ph globe-hemisphere-east >}} Migratieachtergrond


```{r perf-sis-wave2-mig}
# Hoe zorg je ervoor dat je alleen Nederlandse bronnen te zien krijgt? Klik op de plaats in de afbeelding op de plek waar je dit kunt aangeven.
result <- create_stackedbar(
  data = data_filtered_4af682fd %>% tidyr::drop_na(MigrationBackground, PSIS2R),
  title = "Hoe zorg je ervoor dat je alleen Nederlandse bronnen te zien krijgt? Klik op de plaats in de afbeelding op de plek waar je dit kunt aangeven.",
  stacked_type = "percent",
  horizontal = TRUE,
  stack_breaks = c(0, 10, 20, 30),
  stack_bin_labels = c("Incorrect", "Correct"),
  stack_map_values = list("1" = "Correct", "0" = "Incorrect"),
  stack_order = c("Correct", "Incorrect"),
  color_palette = c("#3D7271", "#E28D50", "#F5D76E", "#C7E6D5", "#0F6B5A", "#BABACD"),
  weight_var = "weging_GAMO",
  x_var = "MigrationBackground",
  stack_var = "PSIS2R"
)

result
```


:::


### {{< iconify ph chart-line-fill >}} Verloop over tijd


::: {.panel-tabset}

##### {{< iconify ph users-fill >}} Algemeen


```{r perf-sis-overtime-overall}
# Hoe zorg je ervoor dat je alleen Nederlandse bronnen te zien krijgt? Klik op de plaats in de afbeelding op de plek waar je dit kunt aangeven.
result <- create_timeline(
  data = data,
  title = "Hoe zorg je ervoor dat je alleen Nederlandse bronnen te zien krijgt? Klik op de plaats in de afbeelding op de plek waar je dit kunt aangeven.",
  time_var = "wave_time_label",
  chart_type = "line",
  response_filter = 1,
  y_min = 0,
  y_max = 100,
  response_filter_label = "Percentage die correct heeft geantwoord",
  response_filter_combine = TRUE,
  x_label = "",
  y_label = "Percentage die correct heeft geantwoord",
  color_palette = c("#3D7271", "#E28D50", "#F5D76E", "#C7E6D5", "#0F6B5A", "#BABACD"),
  weight_var = "weging_GAMO",
  response_var = "PSIS2R"
)

result
```


##### {{< iconify mdi:human-male-male-child >}} Leeftijd


```{r perf-sis-overtime-age}
# Hoe zorg je ervoor dat je alleen Nederlandse bronnen te zien krijgt? Klik op de plaats in de afbeelding op de plek waar je dit kunt aangeven.
result <- create_timeline(
  data = data,
  title = "Hoe zorg je ervoor dat je alleen Nederlandse bronnen te zien krijgt? Klik op de plaats in de afbeelding op de plek waar je dit kunt aangeven.",
  time_var = "wave_time_label",
  chart_type = "line",
  response_filter = 1,
  y_min = 0,
  y_max = 100,
  x_label = "",
  y_label = "Percentage die correct heeft geantwoord",
  color_palette = c("#3D7271", "#E28D50", "#F5D76E", "#C7E6D5", "#0F6B5A", "#BABACD"),
  response_filter_label = NULL,
  weight_var = "weging_GAMO",
  response_var = "PSIS2R",
  group_var = "AgeGroup"
)

result
```


##### {{< iconify mdi gender-transgender >}} Geslacht


```{r perf-sis-overtime-gender}
# Hoe zorg je ervoor dat je alleen Nederlandse bronnen te zien krijgt? Klik op de plaats in de afbeelding op de plek waar je dit kunt aangeven.
result <- create_timeline(
  data = data,
  title = "Hoe zorg je ervoor dat je alleen Nederlandse bronnen te zien krijgt? Klik op de plaats in de afbeelding op de plek waar je dit kunt aangeven.",
  time_var = "wave_time_label",
  chart_type = "line",
  response_filter = 1,
  y_min = 0,
  y_max = 100,
  x_label = "",
  y_label = "Percentage die correct heeft geantwoord",
  color_palette = c("#3D7271", "#E28D50", "#F5D76E", "#C7E6D5", "#0F6B5A", "#BABACD"),
  response_filter_label = NULL,
  weight_var = "weging_GAMO",
  response_var = "PSIS2R",
  group_var = "geslacht"
)

result
```


##### {{< iconify ph graduation-cap-fill >}} Opleidingsniveau


```{r perf-sis-overtime-edu}
# Hoe zorg je ervoor dat je alleen Nederlandse bronnen te zien krijgt? Klik op de plaats in de afbeelding op de plek waar je dit kunt aangeven.
result <- create_timeline(
  data = data,
  title = "Hoe zorg je ervoor dat je alleen Nederlandse bronnen te zien krijgt? Klik op de plaats in de afbeelding op de plek waar je dit kunt aangeven.",
  time_var = "wave_time_label",
  chart_type = "line",
  response_filter = 1,
  y_min = 0,
  y_max = 100,
  x_label = "",
  y_label = "Percentage die correct heeft geantwoord",
  color_palette = c("#3D7271", "#E28D50", "#F5D76E", "#C7E6D5", "#0F6B5A", "#BABACD"),
  response_filter_label = NULL,
  weight_var = "weging_GAMO",
  response_var = "PSIS2R",
  group_var = "Education"
)

result
```


##### {{< iconify ph globe-hemisphere-east >}} Migratieachtergrond


```{r perf-sis-overtime-mig}
# Hoe zorg je ervoor dat je alleen Nederlandse bronnen te zien krijgt? Klik op de plaats in de afbeelding op de plek waar je dit kunt aangeven.
result <- create_timeline(
  data = data,
  title = "Hoe zorg je ervoor dat je alleen Nederlandse bronnen te zien krijgt? Klik op de plaats in de afbeelding op de plek waar je dit kunt aangeven.",
  time_var = "wave_time_label",
  chart_type = "line",
  response_filter = 1,
  y_min = 0,
  y_max = 100,
  x_label = "",
  y_label = "Percentage die correct heeft geantwoord",
  color_palette = c("#3D7271", "#E28D50", "#F5D76E", "#C7E6D5", "#0F6B5A", "#BABACD"),
  response_filter_label = NULL,
  weight_var = "weging_GAMO",
  response_var = "PSIS2R",
  group_var = "MigrationBackground"
)

result
```


:::


:::

## {{< iconify ph detective-fill >}} Kritische Informatie


**Kritische Informatievaardigheden** meten het vermogen om online informatie te evalueren: controleren of informatie waar is, betrouwbaarheid van websites beoordelen, en begrijpen wat het doel is van online content (informeren, beïnvloeden, vermaken, of verkopen).

```{r, echo=FALSE, message=FALSE, warning=FALSE}
create_blockquote("Bekijk ook dit bericht goed van sociale media. Welk soort post denk je dat dit is?", preset = "question")
```
[{{< iconify ph cards >}} Bekijk alle Kritische Informatie resultaten](critical_information.html)


::: {.panel-tabset}

### {{< iconify ph number-circle-one-fill >}} Ronde 1


::: {.panel-tabset}

##### {{< iconify ph users-fill >}} Algemeen


```{r perf-cis-wave1-overall}
# 
result <- create_stackedbars(
  data = data_filtered_984a0efe %>% tidyr::drop_na(PCIS1R, PCIS2R, PCIS3R),
  title = "",
  questions = c("PCIS1R", "PCIS2R", "PCIS3R"),
  question_labels = c("Bekijk dit bericht op sociale media goed. Welk soort post denk je dat dit is?", "Bekijk ook dit bericht goed van sociale media. Welk soort post denk je dat dit is?", "Waar moet je naar kijken om te controleren of een bericht op sociale media geen nepnieuws is?"),
  stacked_type = "percent",
  color_palette = c("#3D7271", "#E28D50", "#F5D76E", "#C7E6D5", "#0F6B5A", "#BABACD"),
  horizontal = TRUE,
  x_label = "",
  stack_breaks = c(0, 10, 20, 30),
  stack_bin_labels = c("Incorrect", "Correct"),
  stack_map_values = list("1" = "Correct", "0" = "Incorrect"),
  stack_order = c("Correct", "Incorrect"),
  stack_label = NULL,
  weight_var = "weging_GAMO"
)

result
```


##### {{< iconify mdi:human-male-male-child >}} Leeftijd


::: {.panel-tabset}

###### {{< iconify ph chat-circle-fill >}} Vraag 1


```{r perf-cis-wave1-age-item1}
# Bekijk dit bericht op sociale media goed. Welk soort post denk je dat dit is?
result <- create_stackedbar(
  data = data_filtered_984a0efe %>% tidyr::drop_na(AgeGroup, PCIS1R),
  title = "Bekijk dit bericht op sociale media goed. Welk soort post denk je dat dit is?",
  stacked_type = "percent",
  horizontal = TRUE,
  stack_breaks = c(0, 10, 20, 30),
  stack_bin_labels = c("Incorrect", "Correct"),
  stack_map_values = list("1" = "Correct", "0" = "Incorrect"),
  stack_order = c("Correct", "Incorrect"),
  color_palette = c("#3D7271", "#E28D50", "#F5D76E", "#C7E6D5", "#0F6B5A", "#BABACD"),
  weight_var = "weging_GAMO",
  x_var = "AgeGroup",
  stack_var = "PCIS1R"
)

result
```


###### {{< iconify ph chat-circle-fill >}} Vraag 2


```{r perf-cis-wave1-age-item2}
# Bekijk ook dit bericht goed van sociale media. Welk soort post denk je dat dit is?
result <- create_stackedbar(
  data = data_filtered_984a0efe %>% tidyr::drop_na(AgeGroup, PCIS2R),
  title = "Bekijk ook dit bericht goed van sociale media. Welk soort post denk je dat dit is?",
  stacked_type = "percent",
  horizontal = TRUE,
  stack_breaks = c(0, 10, 20, 30),
  stack_bin_labels = c("Incorrect", "Correct"),
  stack_map_values = list("1" = "Correct", "0" = "Incorrect"),
  stack_order = c("Correct", "Incorrect"),
  color_palette = c("#3D7271", "#E28D50", "#F5D76E", "#C7E6D5", "#0F6B5A", "#BABACD"),
  weight_var = "weging_GAMO",
  x_var = "AgeGroup",
  stack_var = "PCIS2R"
)

result
```


###### {{< iconify ph chat-circle-fill >}} Vraag 3


```{r perf-cis-wave1-age-item3}
# Waar moet je naar kijken om te controleren of een bericht op sociale media geen nepnieuws is?
result <- create_stackedbar(
  data = data_filtered_984a0efe %>% tidyr::drop_na(AgeGroup, PCIS3R),
  title = "Waar moet je naar kijken om te controleren of een bericht op sociale media geen nepnieuws is?",
  stacked_type = "percent",
  horizontal = TRUE,
  stack_breaks = c(0, 10, 20, 30),
  stack_bin_labels = c("Incorrect", "Correct"),
  stack_map_values = list("1" = "Correct", "0" = "Incorrect"),
  stack_order = c("Correct", "Incorrect"),
  color_palette = c("#3D7271", "#E28D50", "#F5D76E", "#C7E6D5", "#0F6B5A", "#BABACD"),
  weight_var = "weging_GAMO",
  x_var = "AgeGroup",
  stack_var = "PCIS3R"
)

result
```


:::


##### {{< iconify mdi gender-transgender >}} Geslacht


::: {.panel-tabset}

###### {{< iconify ph chat-circle-fill >}} Vraag 1


```{r perf-cis-wave1-gender-item1}
# Bekijk dit bericht op sociale media goed. Welk soort post denk je dat dit is?
result <- create_stackedbar(
  data = data_filtered_984a0efe %>% tidyr::drop_na(geslacht, PCIS1R),
  title = "Bekijk dit bericht op sociale media goed. Welk soort post denk je dat dit is?",
  stacked_type = "percent",
  horizontal = TRUE,
  stack_breaks = c(0, 10, 20, 30),
  stack_bin_labels = c("Incorrect", "Correct"),
  stack_map_values = list("1" = "Correct", "0" = "Incorrect"),
  stack_order = c("Correct", "Incorrect"),
  color_palette = c("#3D7271", "#E28D50", "#F5D76E", "#C7E6D5", "#0F6B5A", "#BABACD"),
  weight_var = "weging_GAMO",
  x_var = "geslacht",
  stack_var = "PCIS1R"
)

result
```


###### {{< iconify ph chat-circle-fill >}} Vraag 2


```{r perf-cis-wave1-gender-item2}
# Bekijk ook dit bericht goed van sociale media. Welk soort post denk je dat dit is?
result <- create_stackedbar(
  data = data_filtered_984a0efe %>% tidyr::drop_na(geslacht, PCIS2R),
  title = "Bekijk ook dit bericht goed van sociale media. Welk soort post denk je dat dit is?",
  stacked_type = "percent",
  horizontal = TRUE,
  stack_breaks = c(0, 10, 20, 30),
  stack_bin_labels = c("Incorrect", "Correct"),
  stack_map_values = list("1" = "Correct", "0" = "Incorrect"),
  stack_order = c("Correct", "Incorrect"),
  color_palette = c("#3D7271", "#E28D50", "#F5D76E", "#C7E6D5", "#0F6B5A", "#BABACD"),
  weight_var = "weging_GAMO",
  x_var = "geslacht",
  stack_var = "PCIS2R"
)

result
```


###### {{< iconify ph chat-circle-fill >}} Vraag 3


```{r perf-cis-wave1-gender-item3}
# Waar moet je naar kijken om te controleren of een bericht op sociale media geen nepnieuws is?
result <- create_stackedbar(
  data = data_filtered_984a0efe %>% tidyr::drop_na(geslacht, PCIS3R),
  title = "Waar moet je naar kijken om te controleren of een bericht op sociale media geen nepnieuws is?",
  stacked_type = "percent",
  horizontal = TRUE,
  stack_breaks = c(0, 10, 20, 30),
  stack_bin_labels = c("Incorrect", "Correct"),
  stack_map_values = list("1" = "Correct", "0" = "Incorrect"),
  stack_order = c("Correct", "Incorrect"),
  color_palette = c("#3D7271", "#E28D50", "#F5D76E", "#C7E6D5", "#0F6B5A", "#BABACD"),
  weight_var = "weging_GAMO",
  x_var = "geslacht",
  stack_var = "PCIS3R"
)

result
```


:::


##### {{< iconify ph graduation-cap-fill >}} Opleidingsniveau


::: {.panel-tabset}

###### {{< iconify ph chat-circle-fill >}} Vraag 1


```{r perf-cis-wave1-edu-item1}
# Bekijk dit bericht op sociale media goed. Welk soort post denk je dat dit is?
result <- create_stackedbar(
  data = data_filtered_984a0efe %>% tidyr::drop_na(Education, PCIS1R),
  title = "Bekijk dit bericht op sociale media goed. Welk soort post denk je dat dit is?",
  stacked_type = "percent",
  horizontal = TRUE,
  stack_breaks = c(0, 10, 20, 30),
  stack_bin_labels = c("Incorrect", "Correct"),
  stack_map_values = list("1" = "Correct", "0" = "Incorrect"),
  stack_order = c("Correct", "Incorrect"),
  color_palette = c("#3D7271", "#E28D50", "#F5D76E", "#C7E6D5", "#0F6B5A", "#BABACD"),
  weight_var = "weging_GAMO",
  x_var = "Education",
  stack_var = "PCIS1R"
)

result
```


###### {{< iconify ph chat-circle-fill >}} Vraag 2


```{r perf-cis-wave1-edu-item2}
# Bekijk ook dit bericht goed van sociale media. Welk soort post denk je dat dit is?
result <- create_stackedbar(
  data = data_filtered_984a0efe %>% tidyr::drop_na(Education, PCIS2R),
  title = "Bekijk ook dit bericht goed van sociale media. Welk soort post denk je dat dit is?",
  stacked_type = "percent",
  horizontal = TRUE,
  stack_breaks = c(0, 10, 20, 30),
  stack_bin_labels = c("Incorrect", "Correct"),
  stack_map_values = list("1" = "Correct", "0" = "Incorrect"),
  stack_order = c("Correct", "Incorrect"),
  color_palette = c("#3D7271", "#E28D50", "#F5D76E", "#C7E6D5", "#0F6B5A", "#BABACD"),
  weight_var = "weging_GAMO",
  x_var = "Education",
  stack_var = "PCIS2R"
)

result
```


###### {{< iconify ph chat-circle-fill >}} Vraag 3


```{r perf-cis-wave1-edu-item3}
# Waar moet je naar kijken om te controleren of een bericht op sociale media geen nepnieuws is?
result <- create_stackedbar(
  data = data_filtered_984a0efe %>% tidyr::drop_na(Education, PCIS3R),
  title = "Waar moet je naar kijken om te controleren of een bericht op sociale media geen nepnieuws is?",
  stacked_type = "percent",
  horizontal = TRUE,
  stack_breaks = c(0, 10, 20, 30),
  stack_bin_labels = c("Incorrect", "Correct"),
  stack_map_values = list("1" = "Correct", "0" = "Incorrect"),
  stack_order = c("Correct", "Incorrect"),
  color_palette = c("#3D7271", "#E28D50", "#F5D76E", "#C7E6D5", "#0F6B5A", "#BABACD"),
  weight_var = "weging_GAMO",
  x_var = "Education",
  stack_var = "PCIS3R"
)

result
```


:::


##### {{< iconify ph globe-hemisphere-east >}} Migratieachtergrond


::: {.panel-tabset}

###### {{< iconify ph chat-circle-fill >}} Vraag 1


```{r perf-cis-wave1-mig-item1}
# Bekijk dit bericht op sociale media goed. Welk soort post denk je dat dit is?
result <- create_stackedbar(
  data = data_filtered_984a0efe %>% tidyr::drop_na(MigrationBackground, PCIS1R),
  title = "Bekijk dit bericht op sociale media goed. Welk soort post denk je dat dit is?",
  stacked_type = "percent",
  horizontal = TRUE,
  stack_breaks = c(0, 10, 20, 30),
  stack_bin_labels = c("Incorrect", "Correct"),
  stack_map_values = list("1" = "Correct", "0" = "Incorrect"),
  stack_order = c("Correct", "Incorrect"),
  color_palette = c("#3D7271", "#E28D50", "#F5D76E", "#C7E6D5", "#0F6B5A", "#BABACD"),
  weight_var = "weging_GAMO",
  x_var = "MigrationBackground",
  stack_var = "PCIS1R"
)

result
```


###### {{< iconify ph chat-circle-fill >}} Vraag 2


```{r perf-cis-wave1-mig-item2}
# Bekijk ook dit bericht goed van sociale media. Welk soort post denk je dat dit is?
result <- create_stackedbar(
  data = data_filtered_984a0efe %>% tidyr::drop_na(MigrationBackground, PCIS2R),
  title = "Bekijk ook dit bericht goed van sociale media. Welk soort post denk je dat dit is?",
  stacked_type = "percent",
  horizontal = TRUE,
  stack_breaks = c(0, 10, 20, 30),
  stack_bin_labels = c("Incorrect", "Correct"),
  stack_map_values = list("1" = "Correct", "0" = "Incorrect"),
  stack_order = c("Correct", "Incorrect"),
  color_palette = c("#3D7271", "#E28D50", "#F5D76E", "#C7E6D5", "#0F6B5A", "#BABACD"),
  weight_var = "weging_GAMO",
  x_var = "MigrationBackground",
  stack_var = "PCIS2R"
)

result
```


###### {{< iconify ph chat-circle-fill >}} Vraag 3


```{r perf-cis-wave1-mig-item3}
# Waar moet je naar kijken om te controleren of een bericht op sociale media geen nepnieuws is?
result <- create_stackedbar(
  data = data_filtered_984a0efe %>% tidyr::drop_na(MigrationBackground, PCIS3R),
  title = "Waar moet je naar kijken om te controleren of een bericht op sociale media geen nepnieuws is?",
  stacked_type = "percent",
  horizontal = TRUE,
  stack_breaks = c(0, 10, 20, 30),
  stack_bin_labels = c("Incorrect", "Correct"),
  stack_map_values = list("1" = "Correct", "0" = "Incorrect"),
  stack_order = c("Correct", "Incorrect"),
  color_palette = c("#3D7271", "#E28D50", "#F5D76E", "#C7E6D5", "#0F6B5A", "#BABACD"),
  weight_var = "weging_GAMO",
  x_var = "MigrationBackground",
  stack_var = "PCIS3R"
)

result
```


:::


:::


### {{< iconify ph number-circle-two-fill >}} Ronde 2


::: {.panel-tabset}

##### {{< iconify ph users-fill >}} Algemeen


```{r perf-cis-wave2-overall}
# 
result <- create_stackedbars(
  data = data_filtered_4af682fd %>% tidyr::drop_na(PCIS1R, PCIS2R, PCIS3R),
  title = "",
  questions = c("PCIS1R", "PCIS2R", "PCIS3R"),
  question_labels = c("Bekijk dit bericht op sociale media goed. Welk soort post denk je dat dit is?", "Bekijk ook dit bericht goed van sociale media. Welk soort post denk je dat dit is?", "Waar moet je naar kijken om te controleren of een bericht op sociale media geen nepnieuws is?"),
  stacked_type = "percent",
  color_palette = c("#3D7271", "#E28D50", "#F5D76E", "#C7E6D5", "#0F6B5A", "#BABACD"),
  horizontal = TRUE,
  x_label = "",
  stack_breaks = c(0, 10, 20, 30),
  stack_bin_labels = c("Incorrect", "Correct"),
  stack_map_values = list("1" = "Correct", "0" = "Incorrect"),
  stack_order = c("Correct", "Incorrect"),
  stack_label = NULL,
  weight_var = "weging_GAMO"
)

result
```


##### {{< iconify mdi:human-male-male-child >}} Leeftijd


::: {.panel-tabset}

###### {{< iconify ph chat-circle-fill >}} Vraag 1


```{r perf-cis-wave2-age-item1}
# Bekijk dit bericht op sociale media goed. Welk soort post denk je dat dit is?
result <- create_stackedbar(
  data = data_filtered_4af682fd %>% tidyr::drop_na(AgeGroup, PCIS1R),
  title = "Bekijk dit bericht op sociale media goed. Welk soort post denk je dat dit is?",
  stacked_type = "percent",
  horizontal = TRUE,
  stack_breaks = c(0, 10, 20, 30),
  stack_bin_labels = c("Incorrect", "Correct"),
  stack_map_values = list("1" = "Correct", "0" = "Incorrect"),
  stack_order = c("Correct", "Incorrect"),
  color_palette = c("#3D7271", "#E28D50", "#F5D76E", "#C7E6D5", "#0F6B5A", "#BABACD"),
  weight_var = "weging_GAMO",
  x_var = "AgeGroup",
  stack_var = "PCIS1R"
)

result
```


###### {{< iconify ph chat-circle-fill >}} Vraag 2


```{r perf-cis-wave2-age-item2}
# Bekijk ook dit bericht goed van sociale media. Welk soort post denk je dat dit is?
result <- create_stackedbar(
  data = data_filtered_4af682fd %>% tidyr::drop_na(AgeGroup, PCIS2R),
  title = "Bekijk ook dit bericht goed van sociale media. Welk soort post denk je dat dit is?",
  stacked_type = "percent",
  horizontal = TRUE,
  stack_breaks = c(0, 10, 20, 30),
  stack_bin_labels = c("Incorrect", "Correct"),
  stack_map_values = list("1" = "Correct", "0" = "Incorrect"),
  stack_order = c("Correct", "Incorrect"),
  color_palette = c("#3D7271", "#E28D50", "#F5D76E", "#C7E6D5", "#0F6B5A", "#BABACD"),
  weight_var = "weging_GAMO",
  x_var = "AgeGroup",
  stack_var = "PCIS2R"
)

result
```


###### {{< iconify ph chat-circle-fill >}} Vraag 3


```{r perf-cis-wave2-age-item3}
# Waar moet je naar kijken om te controleren of een bericht op sociale media geen nepnieuws is?
result <- create_stackedbar(
  data = data_filtered_4af682fd %>% tidyr::drop_na(AgeGroup, PCIS3R),
  title = "Waar moet je naar kijken om te controleren of een bericht op sociale media geen nepnieuws is?",
  stacked_type = "percent",
  horizontal = TRUE,
  stack_breaks = c(0, 10, 20, 30),
  stack_bin_labels = c("Incorrect", "Correct"),
  stack_map_values = list("1" = "Correct", "0" = "Incorrect"),
  stack_order = c("Correct", "Incorrect"),
  color_palette = c("#3D7271", "#E28D50", "#F5D76E", "#C7E6D5", "#0F6B5A", "#BABACD"),
  weight_var = "weging_GAMO",
  x_var = "AgeGroup",
  stack_var = "PCIS3R"
)

result
```


:::


##### {{< iconify mdi gender-transgender >}} Geslacht


::: {.panel-tabset}

###### {{< iconify ph chat-circle-fill >}} Vraag 1


```{r perf-cis-wave2-gender-item1}
# Bekijk dit bericht op sociale media goed. Welk soort post denk je dat dit is?
result <- create_stackedbar(
  data = data_filtered_4af682fd %>% tidyr::drop_na(geslacht, PCIS1R),
  title = "Bekijk dit bericht op sociale media goed. Welk soort post denk je dat dit is?",
  stacked_type = "percent",
  horizontal = TRUE,
  stack_breaks = c(0, 10, 20, 30),
  stack_bin_labels = c("Incorrect", "Correct"),
  stack_map_values = list("1" = "Correct", "0" = "Incorrect"),
  stack_order = c("Correct", "Incorrect"),
  color_palette = c("#3D7271", "#E28D50", "#F5D76E", "#C7E6D5", "#0F6B5A", "#BABACD"),
  weight_var = "weging_GAMO",
  x_var = "geslacht",
  stack_var = "PCIS1R"
)

result
```


###### {{< iconify ph chat-circle-fill >}} Vraag 2


```{r perf-cis-wave2-gender-item2}
# Bekijk ook dit bericht goed van sociale media. Welk soort post denk je dat dit is?
result <- create_stackedbar(
  data = data_filtered_4af682fd %>% tidyr::drop_na(geslacht, PCIS2R),
  title = "Bekijk ook dit bericht goed van sociale media. Welk soort post denk je dat dit is?",
  stacked_type = "percent",
  horizontal = TRUE,
  stack_breaks = c(0, 10, 20, 30),
  stack_bin_labels = c("Incorrect", "Correct"),
  stack_map_values = list("1" = "Correct", "0" = "Incorrect"),
  stack_order = c("Correct", "Incorrect"),
  color_palette = c("#3D7271", "#E28D50", "#F5D76E", "#C7E6D5", "#0F6B5A", "#BABACD"),
  weight_var = "weging_GAMO",
  x_var = "geslacht",
  stack_var = "PCIS2R"
)

result
```


###### {{< iconify ph chat-circle-fill >}} Vraag 3


```{r perf-cis-wave2-gender-item3}
# Waar moet je naar kijken om te controleren of een bericht op sociale media geen nepnieuws is?
result <- create_stackedbar(
  data = data_filtered_4af682fd %>% tidyr::drop_na(geslacht, PCIS3R),
  title = "Waar moet je naar kijken om te controleren of een bericht op sociale media geen nepnieuws is?",
  stacked_type = "percent",
  horizontal = TRUE,
  stack_breaks = c(0, 10, 20, 30),
  stack_bin_labels = c("Incorrect", "Correct"),
  stack_map_values = list("1" = "Correct", "0" = "Incorrect"),
  stack_order = c("Correct", "Incorrect"),
  color_palette = c("#3D7271", "#E28D50", "#F5D76E", "#C7E6D5", "#0F6B5A", "#BABACD"),
  weight_var = "weging_GAMO",
  x_var = "geslacht",
  stack_var = "PCIS3R"
)

result
```


:::


##### {{< iconify ph graduation-cap-fill >}} Opleidingsniveau


::: {.panel-tabset}

###### {{< iconify ph chat-circle-fill >}} Vraag 1


```{r perf-cis-wave2-edu-item1}
# Bekijk dit bericht op sociale media goed. Welk soort post denk je dat dit is?
result <- create_stackedbar(
  data = data_filtered_4af682fd %>% tidyr::drop_na(Education, PCIS1R),
  title = "Bekijk dit bericht op sociale media goed. Welk soort post denk je dat dit is?",
  stacked_type = "percent",
  horizontal = TRUE,
  stack_breaks = c(0, 10, 20, 30),
  stack_bin_labels = c("Incorrect", "Correct"),
  stack_map_values = list("1" = "Correct", "0" = "Incorrect"),
  stack_order = c("Correct", "Incorrect"),
  color_palette = c("#3D7271", "#E28D50", "#F5D76E", "#C7E6D5", "#0F6B5A", "#BABACD"),
  weight_var = "weging_GAMO",
  x_var = "Education",
  stack_var = "PCIS1R"
)

result
```


###### {{< iconify ph chat-circle-fill >}} Vraag 2


```{r perf-cis-wave2-edu-item2}
# Bekijk ook dit bericht goed van sociale media. Welk soort post denk je dat dit is?
result <- create_stackedbar(
  data = data_filtered_4af682fd %>% tidyr::drop_na(Education, PCIS2R),
  title = "Bekijk ook dit bericht goed van sociale media. Welk soort post denk je dat dit is?",
  stacked_type = "percent",
  horizontal = TRUE,
  stack_breaks = c(0, 10, 20, 30),
  stack_bin_labels = c("Incorrect", "Correct"),
  stack_map_values = list("1" = "Correct", "0" = "Incorrect"),
  stack_order = c("Correct", "Incorrect"),
  color_palette = c("#3D7271", "#E28D50", "#F5D76E", "#C7E6D5", "#0F6B5A", "#BABACD"),
  weight_var = "weging_GAMO",
  x_var = "Education",
  stack_var = "PCIS2R"
)

result
```


###### {{< iconify ph chat-circle-fill >}} Vraag 3


```{r perf-cis-wave2-edu-item3}
# Waar moet je naar kijken om te controleren of een bericht op sociale media geen nepnieuws is?
result <- create_stackedbar(
  data = data_filtered_4af682fd %>% tidyr::drop_na(Education, PCIS3R),
  title = "Waar moet je naar kijken om te controleren of een bericht op sociale media geen nepnieuws is?",
  stacked_type = "percent",
  horizontal = TRUE,
  stack_breaks = c(0, 10, 20, 30),
  stack_bin_labels = c("Incorrect", "Correct"),
  stack_map_values = list("1" = "Correct", "0" = "Incorrect"),
  stack_order = c("Correct", "Incorrect"),
  color_palette = c("#3D7271", "#E28D50", "#F5D76E", "#C7E6D5", "#0F6B5A", "#BABACD"),
  weight_var = "weging_GAMO",
  x_var = "Education",
  stack_var = "PCIS3R"
)

result
```


:::


##### {{< iconify ph globe-hemisphere-east >}} Migratieachtergrond


::: {.panel-tabset}

###### {{< iconify ph chat-circle-fill >}} Vraag 1


```{r perf-cis-wave2-mig-item1}
# Bekijk dit bericht op sociale media goed. Welk soort post denk je dat dit is?
result <- create_stackedbar(
  data = data_filtered_4af682fd %>% tidyr::drop_na(MigrationBackground, PCIS1R),
  title = "Bekijk dit bericht op sociale media goed. Welk soort post denk je dat dit is?",
  stacked_type = "percent",
  horizontal = TRUE,
  stack_breaks = c(0, 10, 20, 30),
  stack_bin_labels = c("Incorrect", "Correct"),
  stack_map_values = list("1" = "Correct", "0" = "Incorrect"),
  stack_order = c("Correct", "Incorrect"),
  color_palette = c("#3D7271", "#E28D50", "#F5D76E", "#C7E6D5", "#0F6B5A", "#BABACD"),
  weight_var = "weging_GAMO",
  x_var = "MigrationBackground",
  stack_var = "PCIS1R"
)

result
```


###### {{< iconify ph chat-circle-fill >}} Vraag 2


```{r perf-cis-wave2-mig-item2}
# Bekijk ook dit bericht goed van sociale media. Welk soort post denk je dat dit is?
result <- create_stackedbar(
  data = data_filtered_4af682fd %>% tidyr::drop_na(MigrationBackground, PCIS2R),
  title = "Bekijk ook dit bericht goed van sociale media. Welk soort post denk je dat dit is?",
  stacked_type = "percent",
  horizontal = TRUE,
  stack_breaks = c(0, 10, 20, 30),
  stack_bin_labels = c("Incorrect", "Correct"),
  stack_map_values = list("1" = "Correct", "0" = "Incorrect"),
  stack_order = c("Correct", "Incorrect"),
  color_palette = c("#3D7271", "#E28D50", "#F5D76E", "#C7E6D5", "#0F6B5A", "#BABACD"),
  weight_var = "weging_GAMO",
  x_var = "MigrationBackground",
  stack_var = "PCIS2R"
)

result
```


###### {{< iconify ph chat-circle-fill >}} Vraag 3


```{r perf-cis-wave2-mig-item3}
# Waar moet je naar kijken om te controleren of een bericht op sociale media geen nepnieuws is?
result <- create_stackedbar(
  data = data_filtered_4af682fd %>% tidyr::drop_na(MigrationBackground, PCIS3R),
  title = "Waar moet je naar kijken om te controleren of een bericht op sociale media geen nepnieuws is?",
  stacked_type = "percent",
  horizontal = TRUE,
  stack_breaks = c(0, 10, 20, 30),
  stack_bin_labels = c("Incorrect", "Correct"),
  stack_map_values = list("1" = "Correct", "0" = "Incorrect"),
  stack_order = c("Correct", "Incorrect"),
  color_palette = c("#3D7271", "#E28D50", "#F5D76E", "#C7E6D5", "#0F6B5A", "#BABACD"),
  weight_var = "weging_GAMO",
  x_var = "MigrationBackground",
  stack_var = "PCIS3R"
)

result
```


:::


:::


### {{< iconify ph chart-line-fill >}} Verloop over tijd


::: {.panel-tabset}

##### {{< iconify ph users-fill >}} Algemeen


::: {.panel-tabset}

###### {{< iconify ph chat-circle-fill >}} Vraag 1


```{r perf-cis-overtime-overall-item1}
# Bekijk dit bericht op sociale media goed. Welk soort post denk je dat dit is?
result <- create_timeline(
  data = data,
  title = "Bekijk dit bericht op sociale media goed. Welk soort post denk je dat dit is?",
  time_var = "wave_time_label",
  chart_type = "line",
  response_filter = 1,
  y_min = 0,
  y_max = 100,
  response_filter_label = "Percentage die correct heeft geantwoord",
  response_filter_combine = TRUE,
  x_label = "",
  y_label = "Percentage die correct heeft geantwoord",
  color_palette = c("#3D7271", "#E28D50", "#F5D76E", "#C7E6D5", "#0F6B5A", "#BABACD"),
  weight_var = "weging_GAMO",
  response_var = "PCIS1R"
)

result
```


###### {{< iconify ph chat-circle-fill >}} Vraag 2


```{r perf-cis-overtime-overall-item2}
# Bekijk ook dit bericht goed van sociale media. Welk soort post denk je dat dit is?
result <- create_timeline(
  data = data,
  title = "Bekijk ook dit bericht goed van sociale media. Welk soort post denk je dat dit is?",
  time_var = "wave_time_label",
  chart_type = "line",
  response_filter = 1,
  y_min = 0,
  y_max = 100,
  response_filter_label = "Percentage die correct heeft geantwoord",
  response_filter_combine = TRUE,
  x_label = "",
  y_label = "Percentage die correct heeft geantwoord",
  color_palette = c("#3D7271", "#E28D50", "#F5D76E", "#C7E6D5", "#0F6B5A", "#BABACD"),
  weight_var = "weging_GAMO",
  response_var = "PCIS2R"
)

result
```


###### {{< iconify ph chat-circle-fill >}} Vraag 3


```{r perf-cis-overtime-overall-item3}
# Waar moet je naar kijken om te controleren of een bericht op sociale media geen nepnieuws is?
result <- create_timeline(
  data = data,
  title = "Waar moet je naar kijken om te controleren of een bericht op sociale media geen nepnieuws is?",
  time_var = "wave_time_label",
  chart_type = "line",
  response_filter = 1,
  y_min = 0,
  y_max = 100,
  response_filter_label = "Percentage die correct heeft geantwoord",
  response_filter_combine = TRUE,
  x_label = "",
  y_label = "Percentage die correct heeft geantwoord",
  color_palette = c("#3D7271", "#E28D50", "#F5D76E", "#C7E6D5", "#0F6B5A", "#BABACD"),
  weight_var = "weging_GAMO",
  response_var = "PCIS3R"
)

result
```


:::


##### {{< iconify mdi:human-male-male-child >}} Leeftijd


::: {.panel-tabset}

###### {{< iconify ph chat-circle-fill >}} Vraag 1


```{r perf-cis-overtime-age-item1}
# Bekijk dit bericht op sociale media goed. Welk soort post denk je dat dit is?
result <- create_timeline(
  data = data,
  title = "Bekijk dit bericht op sociale media goed. Welk soort post denk je dat dit is?",
  time_var = "wave_time_label",
  chart_type = "line",
  response_filter = 1,
  y_min = 0,
  y_max = 100,
  x_label = "",
  y_label = "Percentage die correct heeft geantwoord",
  color_palette = c("#3D7271", "#E28D50", "#F5D76E", "#C7E6D5", "#0F6B5A", "#BABACD"),
  response_filter_label = NULL,
  weight_var = "weging_GAMO",
  response_var = "PCIS1R",
  group_var = "AgeGroup"
)

result
```


###### {{< iconify ph chat-circle-fill >}} Vraag 2


```{r perf-cis-overtime-age-item2}
# Bekijk ook dit bericht goed van sociale media. Welk soort post denk je dat dit is?
result <- create_timeline(
  data = data,
  title = "Bekijk ook dit bericht goed van sociale media. Welk soort post denk je dat dit is?",
  time_var = "wave_time_label",
  chart_type = "line",
  response_filter = 1,
  y_min = 0,
  y_max = 100,
  x_label = "",
  y_label = "Percentage die correct heeft geantwoord",
  color_palette = c("#3D7271", "#E28D50", "#F5D76E", "#C7E6D5", "#0F6B5A", "#BABACD"),
  response_filter_label = NULL,
  weight_var = "weging_GAMO",
  response_var = "PCIS2R",
  group_var = "AgeGroup"
)

result
```


###### {{< iconify ph chat-circle-fill >}} Vraag 3


```{r perf-cis-overtime-age-item3}
# Waar moet je naar kijken om te controleren of een bericht op sociale media geen nepnieuws is?
result <- create_timeline(
  data = data,
  title = "Waar moet je naar kijken om te controleren of een bericht op sociale media geen nepnieuws is?",
  time_var = "wave_time_label",
  chart_type = "line",
  response_filter = 1,
  y_min = 0,
  y_max = 100,
  x_label = "",
  y_label = "Percentage die correct heeft geantwoord",
  color_palette = c("#3D7271", "#E28D50", "#F5D76E", "#C7E6D5", "#0F6B5A", "#BABACD"),
  response_filter_label = NULL,
  weight_var = "weging_GAMO",
  response_var = "PCIS3R",
  group_var = "AgeGroup"
)

result
```


:::


##### {{< iconify mdi gender-transgender >}} Geslacht


::: {.panel-tabset}

###### {{< iconify ph chat-circle-fill >}} Vraag 1


```{r perf-cis-overtime-gender-item1}
# Bekijk dit bericht op sociale media goed. Welk soort post denk je dat dit is?
result <- create_timeline(
  data = data,
  title = "Bekijk dit bericht op sociale media goed. Welk soort post denk je dat dit is?",
  time_var = "wave_time_label",
  chart_type = "line",
  response_filter = 1,
  y_min = 0,
  y_max = 100,
  x_label = "",
  y_label = "Percentage die correct heeft geantwoord",
  color_palette = c("#3D7271", "#E28D50", "#F5D76E", "#C7E6D5", "#0F6B5A", "#BABACD"),
  response_filter_label = NULL,
  weight_var = "weging_GAMO",
  response_var = "PCIS1R",
  group_var = "geslacht"
)

result
```


###### {{< iconify ph chat-circle-fill >}} Vraag 2


```{r perf-cis-overtime-gender-item2}
# Bekijk ook dit bericht goed van sociale media. Welk soort post denk je dat dit is?
result <- create_timeline(
  data = data,
  title = "Bekijk ook dit bericht goed van sociale media. Welk soort post denk je dat dit is?",
  time_var = "wave_time_label",
  chart_type = "line",
  response_filter = 1,
  y_min = 0,
  y_max = 100,
  x_label = "",
  y_label = "Percentage die correct heeft geantwoord",
  color_palette = c("#3D7271", "#E28D50", "#F5D76E", "#C7E6D5", "#0F6B5A", "#BABACD"),
  response_filter_label = NULL,
  weight_var = "weging_GAMO",
  response_var = "PCIS2R",
  group_var = "geslacht"
)

result
```


###### {{< iconify ph chat-circle-fill >}} Vraag 3


```{r perf-cis-overtime-gender-item3}
# Waar moet je naar kijken om te controleren of een bericht op sociale media geen nepnieuws is?
result <- create_timeline(
  data = data,
  title = "Waar moet je naar kijken om te controleren of een bericht op sociale media geen nepnieuws is?",
  time_var = "wave_time_label",
  chart_type = "line",
  response_filter = 1,
  y_min = 0,
  y_max = 100,
  x_label = "",
  y_label = "Percentage die correct heeft geantwoord",
  color_palette = c("#3D7271", "#E28D50", "#F5D76E", "#C7E6D5", "#0F6B5A", "#BABACD"),
  response_filter_label = NULL,
  weight_var = "weging_GAMO",
  response_var = "PCIS3R",
  group_var = "geslacht"
)

result
```


:::


##### {{< iconify ph graduation-cap-fill >}} Opleidingsniveau


::: {.panel-tabset}

###### {{< iconify ph chat-circle-fill >}} Vraag 1


```{r perf-cis-overtime-edu-item1}
# Bekijk dit bericht op sociale media goed. Welk soort post denk je dat dit is?
result <- create_timeline(
  data = data,
  title = "Bekijk dit bericht op sociale media goed. Welk soort post denk je dat dit is?",
  time_var = "wave_time_label",
  chart_type = "line",
  response_filter = 1,
  y_min = 0,
  y_max = 100,
  x_label = "",
  y_label = "Percentage die correct heeft geantwoord",
  color_palette = c("#3D7271", "#E28D50", "#F5D76E", "#C7E6D5", "#0F6B5A", "#BABACD"),
  response_filter_label = NULL,
  weight_var = "weging_GAMO",
  response_var = "PCIS1R",
  group_var = "Education"
)

result
```


###### {{< iconify ph chat-circle-fill >}} Vraag 2


```{r perf-cis-overtime-edu-item2}
# Bekijk ook dit bericht goed van sociale media. Welk soort post denk je dat dit is?
result <- create_timeline(
  data = data,
  title = "Bekijk ook dit bericht goed van sociale media. Welk soort post denk je dat dit is?",
  time_var = "wave_time_label",
  chart_type = "line",
  response_filter = 1,
  y_min = 0,
  y_max = 100,
  x_label = "",
  y_label = "Percentage die correct heeft geantwoord",
  color_palette = c("#3D7271", "#E28D50", "#F5D76E", "#C7E6D5", "#0F6B5A", "#BABACD"),
  response_filter_label = NULL,
  weight_var = "weging_GAMO",
  response_var = "PCIS2R",
  group_var = "Education"
)

result
```


###### {{< iconify ph chat-circle-fill >}} Vraag 3


```{r perf-cis-overtime-edu-item3}
# Waar moet je naar kijken om te controleren of een bericht op sociale media geen nepnieuws is?
result <- create_timeline(
  data = data,
  title = "Waar moet je naar kijken om te controleren of een bericht op sociale media geen nepnieuws is?",
  time_var = "wave_time_label",
  chart_type = "line",
  response_filter = 1,
  y_min = 0,
  y_max = 100,
  x_label = "",
  y_label = "Percentage die correct heeft geantwoord",
  color_palette = c("#3D7271", "#E28D50", "#F5D76E", "#C7E6D5", "#0F6B5A", "#BABACD"),
  response_filter_label = NULL,
  weight_var = "weging_GAMO",
  response_var = "PCIS3R",
  group_var = "Education"
)

result
```


:::


##### {{< iconify ph globe-hemisphere-east >}} Migratieachtergrond


::: {.panel-tabset}

###### {{< iconify ph chat-circle-fill >}} Vraag 1


```{r perf-cis-overtime-mig-item1}
# Bekijk dit bericht op sociale media goed. Welk soort post denk je dat dit is?
result <- create_timeline(
  data = data,
  title = "Bekijk dit bericht op sociale media goed. Welk soort post denk je dat dit is?",
  time_var = "wave_time_label",
  chart_type = "line",
  response_filter = 1,
  y_min = 0,
  y_max = 100,
  x_label = "",
  y_label = "Percentage die correct heeft geantwoord",
  color_palette = c("#3D7271", "#E28D50", "#F5D76E", "#C7E6D5", "#0F6B5A", "#BABACD"),
  response_filter_label = NULL,
  weight_var = "weging_GAMO",
  response_var = "PCIS1R",
  group_var = "MigrationBackground"
)

result
```


###### {{< iconify ph chat-circle-fill >}} Vraag 2


```{r perf-cis-overtime-mig-item2}
# Bekijk ook dit bericht goed van sociale media. Welk soort post denk je dat dit is?
result <- create_timeline(
  data = data,
  title = "Bekijk ook dit bericht goed van sociale media. Welk soort post denk je dat dit is?",
  time_var = "wave_time_label",
  chart_type = "line",
  response_filter = 1,
  y_min = 0,
  y_max = 100,
  x_label = "",
  y_label = "Percentage die correct heeft geantwoord",
  color_palette = c("#3D7271", "#E28D50", "#F5D76E", "#C7E6D5", "#0F6B5A", "#BABACD"),
  response_filter_label = NULL,
  weight_var = "weging_GAMO",
  response_var = "PCIS2R",
  group_var = "MigrationBackground"
)

result
```


###### {{< iconify ph chat-circle-fill >}} Vraag 3


```{r perf-cis-overtime-mig-item3}
# Waar moet je naar kijken om te controleren of een bericht op sociale media geen nepnieuws is?
result <- create_timeline(
  data = data,
  title = "Waar moet je naar kijken om te controleren of een bericht op sociale media geen nepnieuws is?",
  time_var = "wave_time_label",
  chart_type = "line",
  response_filter = 1,
  y_min = 0,
  y_max = 100,
  x_label = "",
  y_label = "Percentage die correct heeft geantwoord",
  color_palette = c("#3D7271", "#E28D50", "#F5D76E", "#C7E6D5", "#0F6B5A", "#BABACD"),
  response_filter_label = NULL,
  weight_var = "weging_GAMO",
  response_var = "PCIS3R",
  group_var = "MigrationBackground"
)

result
```


:::


:::


:::

## {{< iconify ph palette-fill >}} Digitale Contentcreatie


**Digitale Contentcreatie** vaardigheden omvatten het vermogen om digitale content te maken en aan te passen: presentaties maken, verschillende media combineren, beelden/muziek/video bewerken, en auteursrechten begrijpen rond digitale content.

```{r, echo=FALSE, message=FALSE, warning=FALSE}
create_blockquote('Welk van onderstaande icoontjes is voor het bijsnijden of stukken weghalen van een foto?
', preset = 'question')
```
[{{< iconify ph cards >}} Bekijk alle Digitale Contentcreatie resultaten](digital_content_creation.html)


::: {.panel-tabset}

### {{< iconify ph number-circle-one-fill >}} Ronde 1


::: {.panel-tabset}

##### {{< iconify ph users-fill >}} Algemeen


```{r perf-dccs-wave1-overall}
# Welk van onderstaande icoontjes is voor het bijsnijden of stukken weghalen van een foto?

result <- create_stackedbars(
  data = data_filtered_984a0efe %>% tidyr::drop_na(PDCCS1R),
  title = "Welk van onderstaande icoontjes is voor het bijsnijden of stukken weghalen van een foto?
",
  questions = "PDCCS1R",
  question_labels = "Welk van onderstaande icoontjes is voor het bijsnijden of stukken weghalen van een foto?
",
  stacked_type = "percent",
  color_palette = c("#3D7271", "#E28D50", "#F5D76E", "#C7E6D5", "#0F6B5A", "#BABACD"),
  horizontal = TRUE,
  x_label = "",
  stack_breaks = c(0, 10, 20, 30),
  stack_bin_labels = c("Incorrect", "Correct"),
  stack_map_values = list("1" = "Correct", "0" = "Incorrect"),
  stack_order = c("Correct", "Incorrect"),
  stack_label = NULL,
  weight_var = "weging_GAMO"
)

result
```


##### {{< iconify mdi:human-male-male-child >}} Leeftijd


```{r perf-dccs-wave1-age}
# Welk van onderstaande icoontjes is voor het bijsnijden of stukken weghalen van een foto?

result <- create_stackedbar(
  data = data_filtered_984a0efe %>% tidyr::drop_na(AgeGroup, PDCCS1R),
  title = "Welk van onderstaande icoontjes is voor het bijsnijden of stukken weghalen van een foto?
",
  stacked_type = "percent",
  horizontal = TRUE,
  stack_breaks = c(0, 10, 20, 30),
  stack_bin_labels = c("Incorrect", "Correct"),
  stack_map_values = list("1" = "Correct", "0" = "Incorrect"),
  stack_order = c("Correct", "Incorrect"),
  color_palette = c("#3D7271", "#E28D50", "#F5D76E", "#C7E6D5", "#0F6B5A", "#BABACD"),
  weight_var = "weging_GAMO",
  x_var = "AgeGroup",
  stack_var = "PDCCS1R"
)

result
```


##### {{< iconify mdi gender-transgender >}} Geslacht


```{r perf-dccs-wave1-gender}
# Welk van onderstaande icoontjes is voor het bijsnijden of stukken weghalen van een foto?

result <- create_stackedbar(
  data = data_filtered_984a0efe %>% tidyr::drop_na(geslacht, PDCCS1R),
  title = "Welk van onderstaande icoontjes is voor het bijsnijden of stukken weghalen van een foto?
",
  stacked_type = "percent",
  horizontal = TRUE,
  stack_breaks = c(0, 10, 20, 30),
  stack_bin_labels = c("Incorrect", "Correct"),
  stack_map_values = list("1" = "Correct", "0" = "Incorrect"),
  stack_order = c("Correct", "Incorrect"),
  color_palette = c("#3D7271", "#E28D50", "#F5D76E", "#C7E6D5", "#0F6B5A", "#BABACD"),
  weight_var = "weging_GAMO",
  x_var = "geslacht",
  stack_var = "PDCCS1R"
)

result
```


##### {{< iconify ph graduation-cap-fill >}} Opleidingsniveau


```{r perf-dccs-wave1-edu}
# Welk van onderstaande icoontjes is voor het bijsnijden of stukken weghalen van een foto?

result <- create_stackedbar(
  data = data_filtered_984a0efe %>% tidyr::drop_na(Education, PDCCS1R),
  title = "Welk van onderstaande icoontjes is voor het bijsnijden of stukken weghalen van een foto?
",
  stacked_type = "percent",
  horizontal = TRUE,
  stack_breaks = c(0, 10, 20, 30),
  stack_bin_labels = c("Incorrect", "Correct"),
  stack_map_values = list("1" = "Correct", "0" = "Incorrect"),
  stack_order = c("Correct", "Incorrect"),
  color_palette = c("#3D7271", "#E28D50", "#F5D76E", "#C7E6D5", "#0F6B5A", "#BABACD"),
  weight_var = "weging_GAMO",
  x_var = "Education",
  stack_var = "PDCCS1R"
)

result
```


##### {{< iconify ph globe-hemisphere-east >}} Migratieachtergrond


```{r perf-dccs-wave1-mig}
# Welk van onderstaande icoontjes is voor het bijsnijden of stukken weghalen van een foto?

result <- create_stackedbar(
  data = data_filtered_984a0efe %>% tidyr::drop_na(MigrationBackground, PDCCS1R),
  title = "Welk van onderstaande icoontjes is voor het bijsnijden of stukken weghalen van een foto?
",
  stacked_type = "percent",
  horizontal = TRUE,
  stack_breaks = c(0, 10, 20, 30),
  stack_bin_labels = c("Incorrect", "Correct"),
  stack_map_values = list("1" = "Correct", "0" = "Incorrect"),
  stack_order = c("Correct", "Incorrect"),
  color_palette = c("#3D7271", "#E28D50", "#F5D76E", "#C7E6D5", "#0F6B5A", "#BABACD"),
  weight_var = "weging_GAMO",
  x_var = "MigrationBackground",
  stack_var = "PDCCS1R"
)

result
```


:::


### {{< iconify ph number-circle-two-fill >}} Ronde 2


::: {.panel-tabset}

##### {{< iconify ph users-fill >}} Algemeen


```{r perf-dccs-wave2-overall}
# Welk van onderstaande icoontjes is voor het bijsnijden of stukken weghalen van een foto?

result <- create_stackedbars(
  data = data_filtered_4af682fd %>% tidyr::drop_na(PDCCS1R),
  title = "Welk van onderstaande icoontjes is voor het bijsnijden of stukken weghalen van een foto?
",
  questions = "PDCCS1R",
  question_labels = "Welk van onderstaande icoontjes is voor het bijsnijden of stukken weghalen van een foto?
",
  stacked_type = "percent",
  color_palette = c("#3D7271", "#E28D50", "#F5D76E", "#C7E6D5", "#0F6B5A", "#BABACD"),
  horizontal = TRUE,
  x_label = "",
  stack_breaks = c(0, 10, 20, 30),
  stack_bin_labels = c("Incorrect", "Correct"),
  stack_map_values = list("1" = "Correct", "0" = "Incorrect"),
  stack_order = c("Correct", "Incorrect"),
  stack_label = NULL,
  weight_var = "weging_GAMO"
)

result
```


##### {{< iconify mdi:human-male-male-child >}} Leeftijd


```{r perf-dccs-wave2-age}
# Welk van onderstaande icoontjes is voor het bijsnijden of stukken weghalen van een foto?

result <- create_stackedbar(
  data = data_filtered_4af682fd %>% tidyr::drop_na(AgeGroup, PDCCS1R),
  title = "Welk van onderstaande icoontjes is voor het bijsnijden of stukken weghalen van een foto?
",
  stacked_type = "percent",
  horizontal = TRUE,
  stack_breaks = c(0, 10, 20, 30),
  stack_bin_labels = c("Incorrect", "Correct"),
  stack_map_values = list("1" = "Correct", "0" = "Incorrect"),
  stack_order = c("Correct", "Incorrect"),
  color_palette = c("#3D7271", "#E28D50", "#F5D76E", "#C7E6D5", "#0F6B5A", "#BABACD"),
  weight_var = "weging_GAMO",
  x_var = "AgeGroup",
  stack_var = "PDCCS1R"
)

result
```


##### {{< iconify mdi gender-transgender >}} Geslacht


```{r perf-dccs-wave2-gender}
# Welk van onderstaande icoontjes is voor het bijsnijden of stukken weghalen van een foto?

result <- create_stackedbar(
  data = data_filtered_4af682fd %>% tidyr::drop_na(geslacht, PDCCS1R),
  title = "Welk van onderstaande icoontjes is voor het bijsnijden of stukken weghalen van een foto?
",
  stacked_type = "percent",
  horizontal = TRUE,
  stack_breaks = c(0, 10, 20, 30),
  stack_bin_labels = c("Incorrect", "Correct"),
  stack_map_values = list("1" = "Correct", "0" = "Incorrect"),
  stack_order = c("Correct", "Incorrect"),
  color_palette = c("#3D7271", "#E28D50", "#F5D76E", "#C7E6D5", "#0F6B5A", "#BABACD"),
  weight_var = "weging_GAMO",
  x_var = "geslacht",
  stack_var = "PDCCS1R"
)

result
```


##### {{< iconify ph graduation-cap-fill >}} Opleidingsniveau


```{r perf-dccs-wave2-edu}
# Welk van onderstaande icoontjes is voor het bijsnijden of stukken weghalen van een foto?

result <- create_stackedbar(
  data = data_filtered_4af682fd %>% tidyr::drop_na(Education, PDCCS1R),
  title = "Welk van onderstaande icoontjes is voor het bijsnijden of stukken weghalen van een foto?
",
  stacked_type = "percent",
  horizontal = TRUE,
  stack_breaks = c(0, 10, 20, 30),
  stack_bin_labels = c("Incorrect", "Correct"),
  stack_map_values = list("1" = "Correct", "0" = "Incorrect"),
  stack_order = c("Correct", "Incorrect"),
  color_palette = c("#3D7271", "#E28D50", "#F5D76E", "#C7E6D5", "#0F6B5A", "#BABACD"),
  weight_var = "weging_GAMO",
  x_var = "Education",
  stack_var = "PDCCS1R"
)

result
```


##### {{< iconify ph globe-hemisphere-east >}} Migratieachtergrond


```{r perf-dccs-wave2-mig}
# Welk van onderstaande icoontjes is voor het bijsnijden of stukken weghalen van een foto?

result <- create_stackedbar(
  data = data_filtered_4af682fd %>% tidyr::drop_na(MigrationBackground, PDCCS1R),
  title = "Welk van onderstaande icoontjes is voor het bijsnijden of stukken weghalen van een foto?
",
  stacked_type = "percent",
  horizontal = TRUE,
  stack_breaks = c(0, 10, 20, 30),
  stack_bin_labels = c("Incorrect", "Correct"),
  stack_map_values = list("1" = "Correct", "0" = "Incorrect"),
  stack_order = c("Correct", "Incorrect"),
  color_palette = c("#3D7271", "#E28D50", "#F5D76E", "#C7E6D5", "#0F6B5A", "#BABACD"),
  weight_var = "weging_GAMO",
  x_var = "MigrationBackground",
  stack_var = "PDCCS1R"
)

result
```


:::


### {{< iconify ph chart-line-fill >}} Verloop over tijd


::: {.panel-tabset}

##### {{< iconify ph users-fill >}} Algemeen


```{r perf-dccs-overtime-overall}
# Welk van onderstaande icoontjes is voor het bijsnijden of stukken weghalen van een foto?

result <- create_timeline(
  data = data,
  title = "Welk van onderstaande icoontjes is voor het bijsnijden of stukken weghalen van een foto?
",
  time_var = "wave_time_label",
  chart_type = "line",
  response_filter = 1,
  y_min = 0,
  y_max = 100,
  response_filter_label = "Percentage die correct heeft geantwoord",
  response_filter_combine = TRUE,
  x_label = "",
  y_label = "Percentage die correct heeft geantwoord",
  color_palette = c("#3D7271", "#E28D50", "#F5D76E", "#C7E6D5", "#0F6B5A", "#BABACD"),
  weight_var = "weging_GAMO",
  response_var = "PDCCS1R"
)

result
```


##### {{< iconify mdi:human-male-male-child >}} Leeftijd


```{r perf-dccs-overtime-age}
# Welk van onderstaande icoontjes is voor het bijsnijden of stukken weghalen van een foto?

result <- create_timeline(
  data = data,
  title = "Welk van onderstaande icoontjes is voor het bijsnijden of stukken weghalen van een foto?
",
  time_var = "wave_time_label",
  chart_type = "line",
  response_filter = 1,
  y_min = 0,
  y_max = 100,
  x_label = "",
  y_label = "Percentage die correct heeft geantwoord",
  color_palette = c("#3D7271", "#E28D50", "#F5D76E", "#C7E6D5", "#0F6B5A", "#BABACD"),
  response_filter_label = NULL,
  weight_var = "weging_GAMO",
  response_var = "PDCCS1R",
  group_var = "AgeGroup"
)

result
```


##### {{< iconify mdi gender-transgender >}} Geslacht


```{r perf-dccs-overtime-gender}
# Welk van onderstaande icoontjes is voor het bijsnijden of stukken weghalen van een foto?

result <- create_timeline(
  data = data,
  title = "Welk van onderstaande icoontjes is voor het bijsnijden of stukken weghalen van een foto?
",
  time_var = "wave_time_label",
  chart_type = "line",
  response_filter = 1,
  y_min = 0,
  y_max = 100,
  x_label = "",
  y_label = "Percentage die correct heeft geantwoord",
  color_palette = c("#3D7271", "#E28D50", "#F5D76E", "#C7E6D5", "#0F6B5A", "#BABACD"),
  response_filter_label = NULL,
  weight_var = "weging_GAMO",
  response_var = "PDCCS1R",
  group_var = "geslacht"
)

result
```


##### {{< iconify ph graduation-cap-fill >}} Opleidingsniveau


```{r perf-dccs-overtime-edu}
# Welk van onderstaande icoontjes is voor het bijsnijden of stukken weghalen van een foto?

result <- create_timeline(
  data = data,
  title = "Welk van onderstaande icoontjes is voor het bijsnijden of stukken weghalen van een foto?
",
  time_var = "wave_time_label",
  chart_type = "line",
  response_filter = 1,
  y_min = 0,
  y_max = 100,
  x_label = "",
  y_label = "Percentage die correct heeft geantwoord",
  color_palette = c("#3D7271", "#E28D50", "#F5D76E", "#C7E6D5", "#0F6B5A", "#BABACD"),
  response_filter_label = NULL,
  weight_var = "weging_GAMO",
  response_var = "PDCCS1R",
  group_var = "Education"
)

result
```


##### {{< iconify ph globe-hemisphere-east >}} Migratieachtergrond


```{r perf-dccs-overtime-mig}
# Welk van onderstaande icoontjes is voor het bijsnijden of stukken weghalen van een foto?

result <- create_timeline(
  data = data,
  title = "Welk van onderstaande icoontjes is voor het bijsnijden of stukken weghalen van een foto?
",
  time_var = "wave_time_label",
  chart_type = "line",
  response_filter = 1,
  y_min = 0,
  y_max = 100,
  x_label = "",
  y_label = "Percentage die correct heeft geantwoord",
  color_palette = c("#3D7271", "#E28D50", "#F5D76E", "#C7E6D5", "#0F6B5A", "#BABACD"),
  response_filter_label = NULL,
  weight_var = "weging_GAMO",
  response_var = "PDCCS1R",
  group_var = "MigrationBackground"
)

result
```


:::


:::

## {{< iconify ph chats-fill >}} Netiquette


**Netiquette** verwijst naar correct online communicatie-etiquette: weten wanneer je toestemming moet vragen voor het delen, de juiste communicatietool kiezen, begrijpen wat je niet online moet delen, en emoticons op de juiste manier gebruiken.

```{r, echo=FALSE, message=FALSE, warning=FALSE}
create_blockquote("Prestatietaken: proportie correct of geselecteerd. Bij items met meerdere keuzes tonen we het aandeel dat elke actie selecteert.", preset = "question")
```
[{{< iconify ph cards >}} Bekijk alle Netiquette resultaten](netiquette.html)


::: {.panel-tabset}

### {{< iconify ph number-circle-one-fill >}} Ronde 1


::: {.panel-tabset}

##### {{< iconify ph users-fill >}} Algemeen


```{r perf-netiquette-wave1-overall}
# Stel, je komt deze foto's tegen van een vriendin of kennis op sociale media. Bij welke foto, of foto's, zou je toestemming vragen aan deze vriendin of kennis om deze foto met anderen online te delen? (meerdere antwoorden zijn mogelijk)
result <- create_stackedbars(
  data = data_filtered_984a0efe %>% tidyr::drop_na(PNS1R),
  title = "Stel, je komt deze foto's tegen van een vriendin of kennis op sociale media. Bij welke foto, of foto's, zou je toestemming vragen aan deze vriendin of kennis om deze foto met anderen online te delen? (meerdere antwoorden zijn mogelijk)",
  questions = "PNS1R",
  question_labels = "Stel, je komt deze foto's tegen van een vriendin of kennis op sociale media. Bij welke foto, of foto's, zou je toestemming vragen aan deze vriendin of kennis om deze foto met anderen online te delen? (meerdere antwoorden zijn mogelijk)",
  stacked_type = "percent",
  color_palette = c("#3D7271", "#E28D50", "#F5D76E", "#C7E6D5", "#0F6B5A", "#BABACD"),
  horizontal = TRUE,
  x_label = "",
  stack_breaks = c(0, 10, 20, 30),
  stack_bin_labels = c("Incorrect", "Correct"),
  stack_map_values = list("1" = "Correct", "0" = "Incorrect"),
  stack_order = c("Correct", "Incorrect"),
  stack_label = NULL,
  weight_var = "weging_GAMO"
)

result
```


##### {{< iconify mdi:human-male-male-child >}} Leeftijd


```{r perf-netiquette-wave1-age}
# Stel, je komt deze foto's tegen van een vriendin of kennis op sociale media. Bij welke foto, of foto's, zou je toestemming vragen aan deze vriendin of kennis om deze foto met anderen online te delen? (meerdere antwoorden zijn mogelijk)
result <- create_stackedbar(
  data = data_filtered_984a0efe %>% tidyr::drop_na(AgeGroup, PNS1R),
  title = "Stel, je komt deze foto's tegen van een vriendin of kennis op sociale media. Bij welke foto, of foto's, zou je toestemming vragen aan deze vriendin of kennis om deze foto met anderen online te delen? (meerdere antwoorden zijn mogelijk)",
  stacked_type = "percent",
  horizontal = TRUE,
  stack_breaks = c(0, 10, 20, 30),
  stack_bin_labels = c("Incorrect", "Correct"),
  stack_map_values = list("1" = "Correct", "0" = "Incorrect"),
  stack_order = c("Correct", "Incorrect"),
  color_palette = c("#3D7271", "#E28D50", "#F5D76E", "#C7E6D5", "#0F6B5A", "#BABACD"),
  weight_var = "weging_GAMO",
  x_var = "AgeGroup",
  stack_var = "PNS1R"
)

result
```


##### {{< iconify mdi gender-transgender >}} Geslacht


```{r perf-netiquette-wave1-gender}
# Stel, je komt deze foto's tegen van een vriendin of kennis op sociale media. Bij welke foto, of foto's, zou je toestemming vragen aan deze vriendin of kennis om deze foto met anderen online te delen? (meerdere antwoorden zijn mogelijk)
result <- create_stackedbar(
  data = data_filtered_984a0efe %>% tidyr::drop_na(geslacht, PNS1R),
  title = "Stel, je komt deze foto's tegen van een vriendin of kennis op sociale media. Bij welke foto, of foto's, zou je toestemming vragen aan deze vriendin of kennis om deze foto met anderen online te delen? (meerdere antwoorden zijn mogelijk)",
  stacked_type = "percent",
  horizontal = TRUE,
  stack_breaks = c(0, 10, 20, 30),
  stack_bin_labels = c("Incorrect", "Correct"),
  stack_map_values = list("1" = "Correct", "0" = "Incorrect"),
  stack_order = c("Correct", "Incorrect"),
  color_palette = c("#3D7271", "#E28D50", "#F5D76E", "#C7E6D5", "#0F6B5A", "#BABACD"),
  weight_var = "weging_GAMO",
  x_var = "geslacht",
  stack_var = "PNS1R"
)

result
```


##### {{< iconify ph graduation-cap-fill >}} Opleidingsniveau


```{r perf-netiquette-wave1-edu}
# Stel, je komt deze foto's tegen van een vriendin of kennis op sociale media. Bij welke foto, of foto's, zou je toestemming vragen aan deze vriendin of kennis om deze foto met anderen online te delen? (meerdere antwoorden zijn mogelijk)
result <- create_stackedbar(
  data = data_filtered_984a0efe %>% tidyr::drop_na(Education, PNS1R),
  title = "Stel, je komt deze foto's tegen van een vriendin of kennis op sociale media. Bij welke foto, of foto's, zou je toestemming vragen aan deze vriendin of kennis om deze foto met anderen online te delen? (meerdere antwoorden zijn mogelijk)",
  stacked_type = "percent",
  horizontal = TRUE,
  stack_breaks = c(0, 10, 20, 30),
  stack_bin_labels = c("Incorrect", "Correct"),
  stack_map_values = list("1" = "Correct", "0" = "Incorrect"),
  stack_order = c("Correct", "Incorrect"),
  color_palette = c("#3D7271", "#E28D50", "#F5D76E", "#C7E6D5", "#0F6B5A", "#BABACD"),
  weight_var = "weging_GAMO",
  x_var = "Education",
  stack_var = "PNS1R"
)

result
```


##### {{< iconify ph globe-hemisphere-east >}} Migratieachtergrond


```{r perf-netiquette-wave1-mig}
# Stel, je komt deze foto's tegen van een vriendin of kennis op sociale media. Bij welke foto, of foto's, zou je toestemming vragen aan deze vriendin of kennis om deze foto met anderen online te delen? (meerdere antwoorden zijn mogelijk)
result <- create_stackedbar(
  data = data_filtered_984a0efe %>% tidyr::drop_na(MigrationBackground, PNS1R),
  title = "Stel, je komt deze foto's tegen van een vriendin of kennis op sociale media. Bij welke foto, of foto's, zou je toestemming vragen aan deze vriendin of kennis om deze foto met anderen online te delen? (meerdere antwoorden zijn mogelijk)",
  stacked_type = "percent",
  horizontal = TRUE,
  stack_breaks = c(0, 10, 20, 30),
  stack_bin_labels = c("Incorrect", "Correct"),
  stack_map_values = list("1" = "Correct", "0" = "Incorrect"),
  stack_order = c("Correct", "Incorrect"),
  color_palette = c("#3D7271", "#E28D50", "#F5D76E", "#C7E6D5", "#0F6B5A", "#BABACD"),
  weight_var = "weging_GAMO",
  x_var = "MigrationBackground",
  stack_var = "PNS1R"
)

result
```


:::


### {{< iconify ph number-circle-two-fill >}} Ronde 2


::: {.panel-tabset}

##### {{< iconify ph users-fill >}} Algemeen


```{r perf-netiquette-wave2-overall}
# Stel, je komt deze foto's tegen van een vriendin of kennis op sociale media. Bij welke foto, of foto's, zou je toestemming vragen aan deze vriendin of kennis om deze foto met anderen online te delen? (meerdere antwoorden zijn mogelijk)
result <- create_stackedbars(
  data = data_filtered_4af682fd %>% tidyr::drop_na(PNS1R),
  title = "Stel, je komt deze foto's tegen van een vriendin of kennis op sociale media. Bij welke foto, of foto's, zou je toestemming vragen aan deze vriendin of kennis om deze foto met anderen online te delen? (meerdere antwoorden zijn mogelijk)",
  questions = "PNS1R",
  question_labels = "Stel, je komt deze foto's tegen van een vriendin of kennis op sociale media. Bij welke foto, of foto's, zou je toestemming vragen aan deze vriendin of kennis om deze foto met anderen online te delen? (meerdere antwoorden zijn mogelijk)",
  stacked_type = "percent",
  color_palette = c("#3D7271", "#E28D50", "#F5D76E", "#C7E6D5", "#0F6B5A", "#BABACD"),
  horizontal = TRUE,
  x_label = "",
  stack_breaks = c(0, 10, 20, 30),
  stack_bin_labels = c("Incorrect", "Correct"),
  stack_map_values = list("1" = "Correct", "0" = "Incorrect"),
  stack_order = c("Correct", "Incorrect"),
  stack_label = NULL,
  weight_var = "weging_GAMO"
)

result
```


##### {{< iconify mdi:human-male-male-child >}} Leeftijd


```{r perf-netiquette-wave2-age}
# Stel, je komt deze foto's tegen van een vriendin of kennis op sociale media. Bij welke foto, of foto's, zou je toestemming vragen aan deze vriendin of kennis om deze foto met anderen online te delen? (meerdere antwoorden zijn mogelijk)
result <- create_stackedbar(
  data = data_filtered_4af682fd %>% tidyr::drop_na(AgeGroup, PNS1R),
  title = "Stel, je komt deze foto's tegen van een vriendin of kennis op sociale media. Bij welke foto, of foto's, zou je toestemming vragen aan deze vriendin of kennis om deze foto met anderen online te delen? (meerdere antwoorden zijn mogelijk)",
  stacked_type = "percent",
  horizontal = TRUE,
  stack_breaks = c(0, 10, 20, 30),
  stack_bin_labels = c("Incorrect", "Correct"),
  stack_map_values = list("1" = "Correct", "0" = "Incorrect"),
  stack_order = c("Correct", "Incorrect"),
  color_palette = c("#3D7271", "#E28D50", "#F5D76E", "#C7E6D5", "#0F6B5A", "#BABACD"),
  weight_var = "weging_GAMO",
  x_var = "AgeGroup",
  stack_var = "PNS1R"
)

result
```


##### {{< iconify mdi gender-transgender >}} Geslacht


```{r perf-netiquette-wave2-gender}
# Stel, je komt deze foto's tegen van een vriendin of kennis op sociale media. Bij welke foto, of foto's, zou je toestemming vragen aan deze vriendin of kennis om deze foto met anderen online te delen? (meerdere antwoorden zijn mogelijk)
result <- create_stackedbar(
  data = data_filtered_4af682fd %>% tidyr::drop_na(geslacht, PNS1R),
  title = "Stel, je komt deze foto's tegen van een vriendin of kennis op sociale media. Bij welke foto, of foto's, zou je toestemming vragen aan deze vriendin of kennis om deze foto met anderen online te delen? (meerdere antwoorden zijn mogelijk)",
  stacked_type = "percent",
  horizontal = TRUE,
  stack_breaks = c(0, 10, 20, 30),
  stack_bin_labels = c("Incorrect", "Correct"),
  stack_map_values = list("1" = "Correct", "0" = "Incorrect"),
  stack_order = c("Correct", "Incorrect"),
  color_palette = c("#3D7271", "#E28D50", "#F5D76E", "#C7E6D5", "#0F6B5A", "#BABACD"),
  weight_var = "weging_GAMO",
  x_var = "geslacht",
  stack_var = "PNS1R"
)

result
```


##### {{< iconify ph graduation-cap-fill >}} Opleidingsniveau


```{r perf-netiquette-wave2-edu}
# Stel, je komt deze foto's tegen van een vriendin of kennis op sociale media. Bij welke foto, of foto's, zou je toestemming vragen aan deze vriendin of kennis om deze foto met anderen online te delen? (meerdere antwoorden zijn mogelijk)
result <- create_stackedbar(
  data = data_filtered_4af682fd %>% tidyr::drop_na(Education, PNS1R),
  title = "Stel, je komt deze foto's tegen van een vriendin of kennis op sociale media. Bij welke foto, of foto's, zou je toestemming vragen aan deze vriendin of kennis om deze foto met anderen online te delen? (meerdere antwoorden zijn mogelijk)",
  stacked_type = "percent",
  horizontal = TRUE,
  stack_breaks = c(0, 10, 20, 30),
  stack_bin_labels = c("Incorrect", "Correct"),
  stack_map_values = list("1" = "Correct", "0" = "Incorrect"),
  stack_order = c("Correct", "Incorrect"),
  color_palette = c("#3D7271", "#E28D50", "#F5D76E", "#C7E6D5", "#0F6B5A", "#BABACD"),
  weight_var = "weging_GAMO",
  x_var = "Education",
  stack_var = "PNS1R"
)

result
```


##### {{< iconify ph globe-hemisphere-east >}} Migratieachtergrond


```{r perf-netiquette-wave2-mig}
# Stel, je komt deze foto's tegen van een vriendin of kennis op sociale media. Bij welke foto, of foto's, zou je toestemming vragen aan deze vriendin of kennis om deze foto met anderen online te delen? (meerdere antwoorden zijn mogelijk)
result <- create_stackedbar(
  data = data_filtered_4af682fd %>% tidyr::drop_na(MigrationBackground, PNS1R),
  title = "Stel, je komt deze foto's tegen van een vriendin of kennis op sociale media. Bij welke foto, of foto's, zou je toestemming vragen aan deze vriendin of kennis om deze foto met anderen online te delen? (meerdere antwoorden zijn mogelijk)",
  stacked_type = "percent",
  horizontal = TRUE,
  stack_breaks = c(0, 10, 20, 30),
  stack_bin_labels = c("Incorrect", "Correct"),
  stack_map_values = list("1" = "Correct", "0" = "Incorrect"),
  stack_order = c("Correct", "Incorrect"),
  color_palette = c("#3D7271", "#E28D50", "#F5D76E", "#C7E6D5", "#0F6B5A", "#BABACD"),
  weight_var = "weging_GAMO",
  x_var = "MigrationBackground",
  stack_var = "PNS1R"
)

result
```


:::


### {{< iconify ph chart-line-fill >}} Verloop over tijd


::: {.panel-tabset}

##### {{< iconify ph users-fill >}} Algemeen


```{r perf-netiquette-overtime-overall}
# Stel, je komt deze foto's tegen van een vriendin of kennis op sociale media. Bij welke foto, of foto's, zou je toestemming vragen aan deze vriendin of kennis om deze foto met anderen online te delen? (meerdere antwoorden zijn mogelijk)
result <- create_timeline(
  data = data,
  title = "Stel, je komt deze foto's tegen van een vriendin of kennis op sociale media. Bij welke foto, of foto's, zou je toestemming vragen aan deze vriendin of kennis om deze foto met anderen online te delen? (meerdere antwoorden zijn mogelijk)",
  time_var = "wave_time_label",
  chart_type = "line",
  response_filter = 1,
  y_min = 0,
  y_max = 100,
  response_filter_label = "Percentage die correct heeft geantwoord",
  response_filter_combine = TRUE,
  x_label = "",
  y_label = "Percentage die correct heeft geantwoord",
  color_palette = c("#3D7271", "#E28D50", "#F5D76E", "#C7E6D5", "#0F6B5A", "#BABACD"),
  weight_var = "weging_GAMO",
  response_var = "PNS1R"
)

result
```


##### {{< iconify mdi:human-male-male-child >}} Leeftijd


```{r perf-netiquette-overtime-age}
# Stel, je komt deze foto's tegen van een vriendin of kennis op sociale media. Bij welke foto, of foto's, zou je toestemming vragen aan deze vriendin of kennis om deze foto met anderen online te delen? (meerdere antwoorden zijn mogelijk)
result <- create_timeline(
  data = data,
  title = "Stel, je komt deze foto's tegen van een vriendin of kennis op sociale media. Bij welke foto, of foto's, zou je toestemming vragen aan deze vriendin of kennis om deze foto met anderen online te delen? (meerdere antwoorden zijn mogelijk)",
  time_var = "wave_time_label",
  chart_type = "line",
  response_filter = 1,
  y_min = 0,
  y_max = 100,
  x_label = "",
  y_label = "Percentage die correct heeft geantwoord",
  color_palette = c("#3D7271", "#E28D50", "#F5D76E", "#C7E6D5", "#0F6B5A", "#BABACD"),
  response_filter_label = NULL,
  weight_var = "weging_GAMO",
  response_var = "PNS1R",
  group_var = "AgeGroup"
)

result
```


##### {{< iconify mdi gender-transgender >}} Geslacht


```{r perf-netiquette-overtime-gender}
# Stel, je komt deze foto's tegen van een vriendin of kennis op sociale media. Bij welke foto, of foto's, zou je toestemming vragen aan deze vriendin of kennis om deze foto met anderen online te delen? (meerdere antwoorden zijn mogelijk)
result <- create_timeline(
  data = data,
  title = "Stel, je komt deze foto's tegen van een vriendin of kennis op sociale media. Bij welke foto, of foto's, zou je toestemming vragen aan deze vriendin of kennis om deze foto met anderen online te delen? (meerdere antwoorden zijn mogelijk)",
  time_var = "wave_time_label",
  chart_type = "line",
  response_filter = 1,
  y_min = 0,
  y_max = 100,
  x_label = "",
  y_label = "Percentage die correct heeft geantwoord",
  color_palette = c("#3D7271", "#E28D50", "#F5D76E", "#C7E6D5", "#0F6B5A", "#BABACD"),
  response_filter_label = NULL,
  weight_var = "weging_GAMO",
  response_var = "PNS1R",
  group_var = "geslacht"
)

result
```


##### {{< iconify ph graduation-cap-fill >}} Opleidingsniveau


```{r perf-netiquette-overtime-edu}
# Stel, je komt deze foto's tegen van een vriendin of kennis op sociale media. Bij welke foto, of foto's, zou je toestemming vragen aan deze vriendin of kennis om deze foto met anderen online te delen? (meerdere antwoorden zijn mogelijk)
result <- create_timeline(
  data = data,
  title = "Stel, je komt deze foto's tegen van een vriendin of kennis op sociale media. Bij welke foto, of foto's, zou je toestemming vragen aan deze vriendin of kennis om deze foto met anderen online te delen? (meerdere antwoorden zijn mogelijk)",
  time_var = "wave_time_label",
  chart_type = "line",
  response_filter = 1,
  y_min = 0,
  y_max = 100,
  x_label = "",
  y_label = "Percentage die correct heeft geantwoord",
  color_palette = c("#3D7271", "#E28D50", "#F5D76E", "#C7E6D5", "#0F6B5A", "#BABACD"),
  response_filter_label = NULL,
  weight_var = "weging_GAMO",
  response_var = "PNS1R",
  group_var = "Education"
)

result
```


##### {{< iconify ph globe-hemisphere-east >}} Migratieachtergrond


```{r perf-netiquette-overtime-mig}
# Stel, je komt deze foto's tegen van een vriendin of kennis op sociale media. Bij welke foto, of foto's, zou je toestemming vragen aan deze vriendin of kennis om deze foto met anderen online te delen? (meerdere antwoorden zijn mogelijk)
result <- create_timeline(
  data = data,
  title = "Stel, je komt deze foto's tegen van een vriendin of kennis op sociale media. Bij welke foto, of foto's, zou je toestemming vragen aan deze vriendin of kennis om deze foto met anderen online te delen? (meerdere antwoorden zijn mogelijk)",
  time_var = "wave_time_label",
  chart_type = "line",
  response_filter = 1,
  y_min = 0,
  y_max = 100,
  x_label = "",
  y_label = "Percentage die correct heeft geantwoord",
  color_palette = c("#3D7271", "#E28D50", "#F5D76E", "#C7E6D5", "#0F6B5A", "#BABACD"),
  response_filter_label = NULL,
  weight_var = "weging_GAMO",
  response_var = "PNS1R",
  group_var = "MigrationBackground"
)

result
```


:::


:::



```{=html}
<nav class='pagination-nav pagination-bottom' role='navigation' aria-label='Page navigation'>
  <div class='pagination-container'>
    <button class='pagination-btn pagination-prev pagination-disabled' disabled aria-label='Previous page'>
      <svg class='pagination-icon' width='18' height='18' viewBox='0 0 20 20' fill='none' xmlns='http://www.w3.org/2000/svg'>
        <path d='M12 16L6 10L12 4' stroke='currentColor' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'/>
      </svg>
    </button>
    <div class='pagination-info'>
      <span class='pagination-prefix'></span>
      <input type='number' class='pagination-input' id='page-input-bottom' min='1' max='3' value='1' aria-label='Current page'>
      <span class='pagination-separator'> / 3</span>
    </div>
    <a href='prestatie_p2.html' class='pagination-btn pagination-next' aria-label='Next page'>
      <svg class='pagination-icon' width='18' height='18' viewBox='0 0 20 20' fill='none' xmlns='http://www.w3.org/2000/svg'>
        <path d='M8 16L14 10L8 4' stroke='currentColor' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'/>
      </svg>
    </a>
  </div>
</nav>

<!-- Pagination Navigation Script -->
<script>
(function() {
  const pageUrls = ["prestatie.html","prestatie_p2.html","prestatie_p3.html"];
  const pageInput = document.getElementById('page-input-bottom');
  
  if (pageInput) {
    pageInput.addEventListener('change', function() {
      const pageNum = parseInt(this.value);
      if (pageNum >= 1 && pageNum <= pageUrls.length) {
        window.location.href = pageUrls[pageNum - 1];
      } else {
        this.value = this.getAttribute('value');
      }
    });
    
    pageInput.addEventListener('keypress', function(e) {
      if (e.key === 'Enter') {
        this.blur();
      }
    });
  }
})();
</script>
```

