---
title: "{{< iconify ph chart-line >}} Analysis"
format: html
---

```{r}
#| include: false
library(dashboardr)
```

```{r setup}
#| echo: false
#| warning: false
#| message: false
#| error: false
#| results: 'hide'

# Load required libraries
library(dashboardr)
library(dplyr)
library(highcharter)

# Global chunk options
knitr::opts_chunk$set(
  echo = FALSE,
  warning = FALSE,
  message = FALSE,
  error = FALSE,
  fig.width = 12,
  fig.height = 8,
  dpi = 300
)

# Load data from dataset_small_300obs.rds
data <- readRDS('dataset_small_300obs.rds')

# Data summary
cat('Dataset loaded:', nrow(data), 'rows,', ncol(data), 'columns\n')

```


## Key Demographics

Summary of age distribution...


::: {.callout-note}
All data is anonymized and aggregated
:::

## overview


::: {.panel-tabset}

### Age Distribution

```{r overview}
# Age Distribution
result <- create_histogram(
  data = data,
  title = "Age Distribution",
  color_palette = "#3498DB",
  x_var = "age"
)

result
```


### Income Distribution

```{r overview-2}
# Income Distribution
result <- create_histogram(
  data = data,
  title = "Income Distribution",
  color_palette = "#3498DB",
  x_var = "income"
)

result
```


:::

